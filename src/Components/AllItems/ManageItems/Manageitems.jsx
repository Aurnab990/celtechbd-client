import React from 'react';

const Manageitems = ({product}) => {
    console.log(product);
    return (
        <div>
            <h1>Manage all items</h1>
        </div>
    );
};

export default Manageitems;